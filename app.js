const express = require("express");
const app = express();
const port = 8000;

// Configurations

// Middlewares

// Routes

// Server

app.listen(port, () => {
  console.log(
    `App Started at Port:${port} and the URL is : http://localhost:${port}`
  );
});
